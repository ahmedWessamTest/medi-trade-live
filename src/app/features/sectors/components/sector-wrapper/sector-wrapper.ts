import { Component } from '@angular/core';
import { RouterOutlet } from '@angular/router';

@Component({
  selector: 'app-sector-wrapper',
  imports: [RouterOutlet],
  templateUrl: './sector-wrapper.html',
  styleUrl: './sector-wrapper.css',
})
export class SectorWrapper {}
